// Base (production) environment. Replaced by environment.development.ts during
// `ng serve` / development builds (see angular.json fileReplacements).
export const environment = {
  production: true,
  apiBaseUrl: '/api'
};
